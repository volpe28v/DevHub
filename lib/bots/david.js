var util = require('../util');
module.exports.action = function(data, callback){
  //TODO implements here


  /* Example 
  if (data.msg.match(/David/i)){
    // 返答を生成
    var reply = {};
    reply.name = "David";
    reply.date = util.getFullDate(new Date());
    reply.msg = "@" + data.name + "さん なにか用かい？";

    // 1秒後に返答する
    setTimeout(function(){
      callback(reply);
    },1000);
  }
  */
};
